# 每次推送與 PR 都建置並跑模擬測試（npm run sim），輸出不得出現 ERR／FAIL
name: sim
on: [push, pull_request]
jobs:
  sim:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: npm ci
      - run: npm run sim
